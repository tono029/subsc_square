import type { NextPage } from "next"
import React from "react"

const Posts: NextPage = () => {
  return (
    <div>
      <h2>投稿一覧</h2>

      {/* <ul>
        {posts.map(post) => {
          <li>
            <Link>
              {post.title}
            </Link>
          </li>
        }}
      </ul> */}
    </div>
  )
}

export default Posts;